# Python
.venv/
venv/
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
/dist/
/build/

# Captures and exports are data, not source. Keep them out unless a test
# fixture is deliberately committed under tests/fixtures/.
*.sal
*.bin
*.vcd
*.csv
captures/

# Local agent coordination database (including SQLite WAL/SHM sidecars)
/.squad/

# macOS
.DS_Store

# Repo Skills machine-local install metadata (absolute source path + timestamp)
.claude/skills/repo/.install-local.json
